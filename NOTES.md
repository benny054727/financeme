# FinanceMe — סיכום פרויקט וסשן פיתוח

**מה זה:** אפליקציית ניהול פיננסי אישי בעברית (RTL), PWA, מחוברת לענן.

**קבצים:** `index.html` / `style.css` / `js/*.js` (13 קבצים — פוצל מ-app.js
יחיד; ראו הערה ב-index.html למה לא ES modules אמיתיים) / `sw.js` / `test.html` — בריפו
**`benny054727/financeme`** (GitHub), ענף `claude/mini-personal-project-56d7e3`
שהוא גם ברירת המחדל.

**האתר החי:** https://benny054727.github.io/financeme/

**ענן:** Supabase (פרויקט `loz-shvui-device`), טבלה `financeme_state` (עמודות:
`user_id`, `data` jsonb, `updated_at`), עם Row Level Security + CHECK constraint
שדורש `data.version`. התחברות עם מייל+סיסמה (Supabase Auth).

## מה נבנה בסשנים קודמים (לפני 23/8/2026)

1. **פיצול** מקובץ HTML יחיד ל-3 קבצים + חיבור מלא ל-Supabase (הרשמה/התחברות/
   סנכרון רקע)
2. **תיקוני חישוב מרובים** בעקבות שימוש אמיתי בטלפון — כל התהליך הוביל בסוף
   לנוסחה מאוחדת אחת: **`CALC.monthEnd()`** = יתרה בבנק פחות (הוצאות קבועות +
   משתנות + הלוואות + הפקדה לחיסכון שנרשמו החודש) — זה מקור האמת היחיד שמוצג
   בדף הבית, בדף עו"ש, ובהתראות הקריטיות (מונע מצב ששני מקומות מראים מספרים
   סותרים)
3. **חיסכון אוטומטי** — הוראת קבע אפשר לקשר ליעד חיסכון (`goalId`), וכל חיוב
   חודשי מעדכן אוטומטית את "סה"כ נצבר" ביעד
4. **הפרדה בין "שמור" ל"הפוך להוראת קבע"** בעריכת הוצאה קבועה — עריכת שם/סכום
   לא יוצרת חיוב אוטומטי בטעות; רק כפתור נפרד ומודע עושה זאת
5. **דף הוצאות שופץ** — עוגת "פירוט לפי קטגוריה" (לחיצה על פלח פותחת חלונית
   עם תקציב-מול-בפועל) + רשימת "תנועות" לפי שם, במקום רשימת קטגוריות ארוכה
6. **שלבי אבטחת נתונים** (מתוך חוות דעת מקצועית שניתנה על הפרויקט):
   - `test.html` — 17 בדיקות אוטומטיות למנוע החישוב (יתרה, זמינות, סיכום
     חודשי, תאריך חיוב, הלוואות, תחזית מאוחדת) — **תמיד לבדוק שם ירוק אחרי
     שינוי בנוסחאות**
   - זיהוי התנגשות סנכרון בין מכשירים (סמל 🔀 ליד ⚙️)
   - **באג קריטי שתוקן:** רענון דף מיד אחרי שמירה (לפני שהסנכרון לענן הספיק)
     היה דורס שינויים מקומיים בגרסה ישנה מהענן. תוקן עם חותמת זמן מקומית
     (`meta.localSavedAt`) שמשווים מול הענן בטעינה

## סשן 23/8/2026 — סיכום מלא (הכי עדכני, קרא קודם)

**התחלה:** התיקייה לא הייתה מחוברת בפועל ל-git (אין `.git`) למרות שהקבצים
היו שם — חוברה מחדש (`git init`+`remote add`+`fetch`), אומת שהתוכן המקומי
זהה בית-לבית לענף המרוחק לפני checkout.

**תיקון:** מחיקת יעד חיסכון (`delGoal`) השאירה תנועת הפקדה יתומה של החודש
הנוכחי (אותה משפחת באג כמו `delRec` הישן) — תוקן + תיקון רטרואקטיבי ב-`migrate()`.

**ביקורת מקצועית מלאה** פורסמה כ-Artifact (חוזקות/חולשות/עיצוב), והוסכמה
תוכנית מתוך "מה הייתי עושה קודם": (1) נגישות, (2) מחיקת חשבון+אזהרת גיבוי,
(3) פיצול app.js, (4) גרף מגמה, (5) לפי צורך בפועל (לא מומש מראש בכוונה).
כל 4 הראשונים בוצעו:

1. **נגישות** — `aria-label` על כל כפתור-אייקון-בלבד, `--faint` הוכהה
   (היה מתחת ל-WCAG AA), `sheet()` קיבל `role="dialog"`+פוקוס+לכידת Tab+Esc.
2. **מחיקת ענן** — `deleteCloudData()` (מוחק את `financeme_state`, לא את
   משתמש ה-Auth עצמו — Supabase לא מאפשר את זה מ-client-side בלי service
   role) + אזהרה לפני `exportDB()` שהקובץ לא מוצפן.
3. **פיצול app.js** (1,988 שורות) ל-13 קבצים תחת `js/` — ראו הטבלה למעלה.
   חולץ **סקריפטית** (PowerShell, לא ידני) עם וידוא reconstruction בית-לבית
   מול המקור; באג אמיתי שנתפס תוך כדי: חיתוך בתוך בלוק הערה מרובה-שורות
   (תוקן ע"י מיפוי כל טווחי ה-`/* */` ווידוא שאף גבול לא נופל בתוכם).
4. **גרף "מגמת הוצאות"** בדף הבית — עבר כמה איטרציות (ראו "עיצוב" למטה).

**עיצוב ניווט דסקטופ (סדרת שינויים ארוכה, כל אחד commit נפרד):**
סיידבר אנכי ימני → פס אופקי צף מעל ההדר → פס מלא-רוחב בשורה עליונה →
**דביק בגלילה** (`position:fixed`, לא `absolute` — המשתמש ביקש לא לגלול
חזרה למעלה כדי לנווט) → הלוגו עבר למקום כפתור ה-"+" (בתוך ה-nav עצמו,
`.nav-brand`), וה-"+" עבר להיות אחרי "חיסכון" → ה-`header` כווץ להתאים
בול לכותרת (`padding-bottom` 74px→22px, כי כבר אין שורת מותג שצריך מרווח
בשבילה).
**נקודות טכניות קריטיות אם נוגעים בזה שוב:**
- `.hbtns` (הגדרות/סנכרון) הוא `position:fixed` (לא `absolute`) — **חייב**
  לזוז יחד עם ה-nav הדביק, אחרת בגלילה הם נשארים מאחור ונעלמים
- `nav{z-index:20}` (לא 1!) — `main{}` הוא `z-index:10`, ובלי זה כרטיסים
  שגוללים מתחת לרצועה הדביקה מכסים אותה
- `.nav-brand` הוא **עותק נפרד** מ-`.brand` — קיים תמיד ב-DOM, ה-CSS מציג
  רק אחד מהם לפי רוחב מסך (`header .brand{display:none}` בדסקטופ בלבד)

**חיסכון/הוראות קבע — הבהרות ותכונות:**
- הוסבר למשתמש (ותועד ב-UI): "כבר נצבר"/"הפרשה חודשית" ביעד הם **תכנון
  בלבד**, לא תנועה — לא משנים את יתרת העו"ש. רק **הוראת קבע מקושרת ל-goalId**
  יוצרת ירידה אמיתית וחוזרת. נוסף כפתור "+ צור הוראת קבע לחיסכון הזה"
  במסך עריכת יעד (`openRecurring` קיבל פרמטרים חדשים `presetGoalId`/`presetAmt`).
- `openGoalEdit`/`saveGoalEdit` חדשים — עריכת יעד קיים כולל "תאריך ההפקדה
  הראשונית" (`createdDate`, נקודת העיגון של גרף הצמיחה).
- **עדכון אוטומטי של יתרת הבנק** (`openingBalance`) כש-`genRecurring()`
  יוצרת תנועה חדשה עם `chargeDate<=openingDate` (כלומר "לפני" עוגן היתרה
  הידני) — עם טוסט הסבר. תוקן גם `toast()` להערים טוסטים זה מעל זה (היו
  חופפים בול כששני טוסטים מוצגים ברצף).

**דף הוצאות/חיסכון:**
- טאב "חיסכון" רביעי (ליד קבועות/משתנות/הכנסות) — דרש הוספת `byCat` גם
  לתנועות חיסכון ב-`CALC.month()` (לא היה שם קודם, רק `r.saving` כללי).
- "ניהול הוראות קבע" מסונן עכשיו גם לפי קטגוריית ההוראה (לא רק כיוון) —
  לפני זה הוראת חיסכון הופיעה גם תחת "קבועות" בטעות.
- **בורר שנה+חודש מודרני** (`.ybar`+`shiftPickerYear`) במקום "6 חודשים
  אחרונים" קבוע יחסית להיום — עכשיו ניווט שנים מלא, 12 חודשים לשנה נבחרת.
- טאבים קבועות/משתנות/חיסכון/הכנסות: class **חדש** `.tabcards` (כרטיסים
  ממוסגרים, כמו `.kcard`) — **לא** `.seg` הקיים, כי `.seg` עדיין בשימוש
  במקומות אחרים (טופס תנועה, הוראת קבע) עם 2 כפתורים ומצפה לסגנון "פיל".

**גרף מגמה (הכי הרבה סבבי משוב/איטרציות):** הוצאות (אדום) + חיסכון (סגול)
זו-לצד-זו לכל חודש, **לא** מוערמות (חיסכון לא "עוד סוג הוצאה"). שרשרת
תיקונים: (א) תוויות סכום קבועות בשתי שורות עליונות במקום לפי גובה כל
עמודה — פתר "לא מיושר בין חודשים"; (ב) `max-width` על ה-SVG כדי למנוע
התנפחות בכרטיס רחב (SVG עם `width:100%;height:auto` על viewBox קבוע
מתנפח כולל הטקסט שבפנים, ככל שהכרטיס רחב יותר — כמו `.dsvg` של העוגה
שכבר משתמשת בגודל קבוע); (ג) בסוף — **הוסרו הסכומים מתוך ה-SVG לגמרי**
ועברו לשורת המקרא כ-HTML רגיל (span צבעוני), מה שפתר את בעיית ההתנפחות
מהשורש (בלי טקסט ב-SVG, אין מה שיתנפח) ואיפשר להחזיר את הגרף לרוחב מלא.

**הגדרות חשבון:** שם פרטי/משפחה (ב-`user_metadata` של Supabase Auth,
לא `DB.settings` — מסונכרן דרך ההתחברות עצמה) + שינוי סיסמה (מינימום 8) +
"התנתק מכל המכשירים" (`sb.auth.signOut({scope:'global'})`) + מינימום
סיסמה בהרשמה עלה 6→8 לעקביות.

## סשן 24/8/2026 (המשך) — הלוואות: אמצעי תשלום + תנועות אמיתיות

**מה ביקש המשתמש:** להגדיר לכל הלוואה אם היא יורדת ישירות מהעו"ש או מחויבת
בכרטיס אשראי, ושכל הלוואה תופיע בפועל ב"תנועות שכבר ירדו" (אם מהעו"ש) או
ב"חיובים והכנסות צפויים" (אם מכרטיס) בדף עו"ש.

**למה זה לא טריוויאלי:** לפני זה הלוואה הייתה **רק חישוב חי** (`LOANS.
allMonthlyTotal()`), בלי שום תנועה אמיתית ב-`DB.transactions` — בכוונה, כדי
שמשתמש שכבר עוקב אחרי ההלוואה כהוראת קבע נפרדת (`settings.loansAffectBalance`)
לא יספור פעמיים. כדי שהלוואה "תופיע ברשימות" היה צריך שתהיה לה תנועה אמיתית —
אבל זה פותח סיכון כפילות חדש: `av.balance` (מ-`CALC.balance()`) היה מתחיל
לכלול את תשלום ההלוואה ברגע שה-`chargeDate` שלו עובר, בזמן ש-`monthEnd()`/
`available()` המשיכו להוסיף את `LOANS.allMonthlyTotal()`/`loanPending()` בנפרד
— כפילות אמיתית.

**הפתרון שיושם:**
1. `DB.loans[].paymentMethod` ('account'/'card') + `cardId` — נבחר בטופס
   הלוואה חדש/עריכה (`openLoanForm`, `js/sheets-recurring-goals.js`), עם בורר
   כרטיס שמופיע/נעלם דינמית (`paintLoanCardField`).
2. **`genLoanPayments()`** (`js/recurring.js`) — בדיוק אותו מנגנון כמו
   `genRecurring()` (אותם 14 חודשים, אותו `anchorFold`, אותו רעיון "מזהה קבוע
   = אין כפילות"): יוצר תנועה אמיתית לכל הלוואה כל חודש, עם `chargeDate=d`
   ל"עו"ש" או `CALC.chargeDate(d,card)` ל"כרטיס" (בדיוק כמו הוראת קבע בכרטיס).
   פעיל רק אם `loansAffectBalance` (כמו קודם). נקרא מ-`boot()` אחרי
   `genRecurring()`, וגם אחרי שמירת/מחיקת הלוואה ואחרי שינוי ההגדרה עצמה.
3. קטגוריה חדשה `c_loan` (kind:`'loan'`) — `CALC.month()` סופר אותה בדלי
   נפרד (`r.loan`), לא ב-`r.out`/`fixed`/`variable`, בדיוק כמו `saving`.
4. **הוסר `CALC.loanPending()`** — מיותר עכשיו: תשלום הלוואה שכבר תנועה
   אמיתית כבר נכלל ב-`pending`/`balance` דרך הלולאה הרגילה על `DB.transactions`.
5. `monthEnd()`/`vHome`/`alerts()`: `loanPay` עבר מ-`LOANS.allMonthlyTotal()`
   החי ל-`m.loan` (מתנועות אמיתיות של החודש) — כדי לא לסתור את `av.balance`
   שכבר כולל את מה שבאמת ירד. **`LOANS.allMonthlyTotal()` נשאר** בשימוש
   במקומות תיאוריים-בלבד (קופסת "הלוואות" בדף עו"ש, `loanDetail()`) — "מה
   ההחזר הנוכחי לפי התנאים", לא קשור לספירת יתרה.
6. `delLoan()` מנקה תנועה שכבר נוצרה החודש הנוכחי, אותו עיקרון בדיוק כמו
   `delRec()`/`delGoal()` (עריכה/מחיקה משפיעה קדימה בלבד, היסטוריה נשארת).

**תוצאה שנבדקה בפועל** (הזרקת DB דרך הדפדפן, לא רק test.html): הלוואה
"מהעו"ש" עם יום חיוב שכבר עבר מופיעה ב"תנועות שכבר ירדו"; הלוואה בכרטיס
נדחית לתאריך החיוב של הכרטיס ומופיעה ב"חיובים והכנסות צפויים" **וגם**
בדף הכרטיס עצמו (`vCards`/`cycle()`) כחלק מהחיוב הקרוב — בונוס, לא התבקש
במפורש אבל נובע באופן טבעי מזה שזו תנועה אמיתית לכל דבר. `monthEnd()`/
`יתרה בבנק` נבדקו ידנית שלא כופלים (18,530 יתרה, 17,227 תחזית עם 2 הלוואות
במקביל — התאמה מדויקת לחישוב ידני). `loansAffectBalance=false` נבדק שלא
יוצר שום תנועה. `test.html` עודכן (19 בדיקות עכשיו, לא 17) — תרחיש "תחזית
לסוף החודש" מדמה ידנית תנועת הלוואה שכבר "נוצרה" (כי הבדיקות בכוונה לא
קוראות לפונקציות שקוראות ל-`save()`, ראו הערה בראש test.html).

## סשן 24/8/2026 (המשך 2) — מחיקת הלוואה השאירה תנועות יתומות שהמשיכו להשפיע על היתרה

**דיווח המשתמש:** מחק הלוואות (רכב + קרן מילואים) אבל הן עדיין הופיעו
בתנועות, והיתרה המוצגת המשיכה להיות מחושבת כאילו ההלוואות עדיין קיימות.

**הסיבה:** `delLoan()` (גרסה קודמת) חיקתה את `delRec()`/`delGoal()` — מוחקת
רק את תשלום **החודש הנוכחי**, משאירה חודשים קודמים בהיסטוריה בכוונה
("עריכה משפיעה קדימה בלבד"). אבל `genLoanPayments()` תמיד מייצר גם חודש
אחד אחורה (בדיוק כמו `genRecurring()`), אז ברגע שהגדרת הלוואה חדשה כבר יש
לה תנועת "חודש שעבר" שנשארת יתומה לצמיתות אחרי מחיקה — וממשיכה להיספר
ב-`CALC.balance()` (שסופרת כל תנועה עם `chargeDate<=היום`, בלי קשר לקיום
ההלוואה שיצרה אותה).

**התיקון (שינוי החלטה מהותי):** `delLoan()` עכשיו מוחקת את **כל** התשלומים
של ההלוואה, כולל עבר — לא רק החודש הנוכחי. הנימוק: בניגוד להוצאה קבועה/יעד
(תנועות שהמשתמש מודע להן אחת-אחת), תשלומי הלוואה הם "סדרה" שנוצרה כולה
אוטומטית ע"י המערכת מרגע הגדרת ההלוואה — קרוב יותר מבחינה מושגית ל
-`deleteInstallmentGroup()` (מחיקת תוכנית תשלומים כוללת עבר) מאשר ל-`delRec()`.

**תיקון רטרואקטיבי ב-`migrate()`** (`data.js`) — מנקה אוטומטית, בטעינה
הבאה, כל תנועה עם `loanId` שלא שייכת לאף הלוואה קיימת (לא רק החודש הנוכחי
כמו התיקון המקביל ל-`delGoal`) — **זה מה שיתקן את הנתונים הקיימים של
המשתמש בפועל**, בלי שיצטרך למחוק תנועות ידנית אחת-אחת.

**מגבלה ידועה שנשארה (לא ספציפית להלוואות — קיימת גם בהוראות קבע):** אם
תנועת הלוואה נוצרה עם `chargeDate<=openingDate` בזמן היצירה, הסכום שלה
כבר "התקפל" (`anchorFold`) ישירות לתוך `DB.account.openingBalance` בתור
עדכון יתרה חד-פעמי (ראו `genLoanPayments`/`genRecurring`) — זה לא ניתן
ל"ביטול" אוטומטי כשמוחקים את התנועה מאוחר יותר, כי `openingBalance` הוא
מספר יחיד בלי זיכרון של מה תרם לו. אם אחרי העדכון היתרה עדיין נראית לא
נכונה — הפתרון הרגיל שכבר קיים באפליקציה: "עדכן יתרה מהבנק" עם היתרה
האמיתית מהבנק.

## סשן 24/8/2026 (המשך 3) — אותה בעיה בדיוק ביעד/קרן חיסכון שנמחקו

**דיווח המשתמש:** אחרי תיקון ההלוואות, אותה תופעה בדיוק דווחה על "קרן עתיד"
שנמחקה — היתרה המוצגת המשיכה לשקף הפקדה של 1,500 שכבר "נמחקה".

**הסיבה — אותה משפחת באג, שלישית:** `delGoal()` ניקתה רק הפקדות של החודש
הנוכחי (כמו `delRec()` הישן, `delLoan()` הישן) — הפקדה ידנית מחודש קודם
נשארה יתומה, ממשיכה להיספר ב-`CALC.balance()`/`CALC.month().saving`.

**התיקון — עדין יותר מזה של הלוואות, כי ביעד יש שני סוגי תנועה שונים:**
- הפקדה **ידנית** (`doDeposit`, בלי `recurringId`) — קיימת רק בגלל היעד
  הזה. נמחקת לגמרי במחיקת היעד, כולל מהעבר (כמו הלוואה).
- הפקדה מ**הוראת קבע מקושרת** (`recurringId` מוגדר) — ההוראה ממשיכה לרוץ
  קדימה גם אחרי מחיקת היעד (רק הקישור מתנתק, זו התנהגות קיימת ומכוונת).
  מחיקת ההיסטוריה שלה הייתה יוצרת "חור" מלאכותי מול מה שממשיך להירשם
  לאותה הוראה בחודשים הבאים — **נשארת**, רק `goalId` מתנקה ל-`null`.

תיקון רטרואקטיבי מקביל ב-`migrate()` (`data.js`) — מנקה לפי אותו כלל
בטעינה הבאה, בלי צורך בפעולה ידנית מהמשתמש. נבדק בדפדפן: הפקדה ידנית
מחודש קודם + מהחודש הנוכחי לשני "קרן עתיד" יתומה → שתיהן נמחקות,
היתרה חוזרת למדויק. הפקדה מהוראת קבע מקושרת ליעד שנמחק → נשארת בהיסטוריה,
רק `goalId` מתנקה, ההוראה ממשיכה לרוץ. `test.html` עדיין 19/19.

## סשן 25/8/2026 (המשך) — גרף המגמה בדף הבית לא כלל הלוואות ב"הוצאות"

**דיווח המשתמש:** בכרטיסיית "מגמת הוצאות וחיסכון" בדף הבית, "הוצאות" הציג
5,325 בלי החזר ההלוואה (1,190), בזמן שכרטיסיית ה-KPI "הוצאות" למעלה כן
כללה אותו (8,015 = 5,325+1,190+1,500).

**הסיבה:** באג שנוצר כתוצאה-לוואי מפיצ'ר ההלוואות — `expenseTrendChart()`
(views-home.js) חישבה `expVals` לפי `CALC.month(y).out` בלבד. זה היה נכון
לפני שתשלומי הלוואה הפכו לתנועות אמיתיות (`genLoanPayments`), כי אז
`.out` כלל בפועל את כל מה ש"יצא" באמת. אחרי הפיצ'ר, הלוואה קיבלה דלי
נפרד משלה (`.loan`, לא נכלל יותר ב-`.out` — בדיוק כמו `.saving`) — וכל
מקום אחר שמחשב "הוצאות" עודכן בהתאם (`+loanPay`/`+m.loan`) **חוץ** מהגרף
הזה, שנשכח.

**התיקון:** `expVals=months.map(y=>{const mm=CALC.month(y);return mm.out+mm.loan;})`
— **לא** `+saving` (חיסכון כבר מוצג כעמודות נפרדות משלו בגרף עצמו, בדיוק
כמו ב"חלוקת החודש"/תחזית — לצרף אותו ל"הוצאות" גם כאן היה כפילות ויזואלית).
נבדק בדפדפן עם תרחיש שמשחזר בדיוק את המספרים מהדיווח (5,325 משתנות +
1,190 הלוואה) — הגרף מציג עכשיו "הוצאות ₪6,515" נכון. `test.html` 19/19
(לא נגע ב-calc.js).

## סשן 25/8/2026 (המשך 2) — כניסה לתשלום הלוואה: אין עריכה + סכום מכוער

**דיווח המשתמש:** נכנס לתנועת תשלום הלוואה, אין כפתור עריכה (רק העורך
הגנרי של תנועה בודדת), והסכום מוצג כ-`616.7358833007362`. ביקש שעריכה
תאפשר לשנות הכל כולל אמצעי התשלום (עו"ש/כרטיס).

**שני באגים נפרדים, שניהם תוצאת-לוואי של פיצ'ר ההלוואות:**
1. **`openTxSmart()`** (sheets-entry.js) לא ידעה על `kind==='loan'` בכלל —
   נפלה תמיד לעורך הגנרי (`openTx`), בלי שום קישור לעריכת ההלוואה עצמה.
   תוקן באותו עיקרון בדיוק כמו "קבועה עם הוראת קבע פעילה": מנתבים ל-
   `loanDetail(x.loanId)` (יש לו כבר כפתור "ערוך" ←`openLoanForm`, כולל
   אמצעי תשלום/כרטיס — בדיוק מה שהמשתמש ביקש). אם ההלוואה כבר נמחקה
   (תנועה יתומה מההיסטוריה) — נופל בחזרה לעורך הגנרי, לא קורס.
2. **הסכום לא היה מעוגל** — `genLoanPayments()` שמרה את התוצאה הגולמית
   של נוסחת שפיצר (כמעט אף פעם לא עגולה). `fmt()` מעגל לתצוגה בכל מקום
   אחר, אבל שדה `<input type="number">` בעורך הגנרי מציג את הערך הגולמי.
   תוקן ב-2 מקומות: `Math.round()` בייצור תנועה חדשה, **ותיקון רטרואקטיבי
   ב-`migrate()`** שמעגל כל תנועת הלוואה קיימת עם `loanId` — יתקן אוטומטית
   את התנועות הקיימות של המשתמש בטעינה הבאה.

נבדק בדפדפן: קרן לא-עגולה (7401.23, ריבית 3.7%) → תנועה נוצרת עגולה
לשקל שלם; סימולציית תנועה ישנה עם 616.7358833007362 → `migrate()` מעגלת
ל-617; קליק על תנועת הלוואה → נפתח `loanDetail` עם כפתור "ערוך" שמוביל
לטופס המלא (אמצעי תשלום+כרטיס); תנועה יתומה (הלוואה שנמחקה) → נופלת
בבטחה לעורך הגנרי בלי קריסה. `test.html` 19/19.

## סשן 25/8/2026 — טאב "הלוואות" חדש בדף הוצאות

**בקשה:** להוסיף לשורת הטאבים בדף הוצאות (קבועות/משתנות/חיסכון/הכנסות)
גם טאב "הלוואות" עם נתוני ההלוואות בנפרד.

**מימוש:** טאב חמישי `expTab='loan'` ב-`vExpenses()` (`views-expenses-savings.js`).
ההבדל מהטאבים האחרים: **הפירוט הוא לפי הלוואה בודדת, לא לפי קטגוריה** —
כל תשלומי ההלוואות חולקים קטגוריה אחת (`c_loan`), אז עוגה "לפי קטגוריה"
הייתה מראה פלח אחד ענק בלי תועלת. הפירוט נבנה ישירות מהתנועות
(`note`+`loanId`), **לא** מ-`DB.loans` — כך שגם הלוואה שכבר נמחקה ממשיכה
להופיע נכון בהיסטוריה של חודשים קודמים (השם נשמר כ-`note` בכל תנועה
שנוצרה ב-`genLoanPayments`, בדיוק בשביל המקרה הזה). לחיצה על פלח/שורה
בעוגה פותחת את `loanDetail()` הקיים (רק אם ההלוואה עדיין קיימת).

**CSS:** `.tabcards` היה בנוי לרוחב קבוע ל-4 עמודות (`repeat(4,1fr)`) — עם
5 טאבים זה היה שובר את הפריסה. שונה ל-`repeat(auto-fit,minmax(56px,1fr))`
+ הוקטן מעט הפונט (11.5px→10.5px, סכום 13.5px→12px) כדי שכל 5 הטאבים
יכנסו בשורה אחת גם ב-375px בלי גלישה (נבדק: כל כפתור ~63px, אין חיתוך טקסט).

## סשן 24/8/2026 — שני תיקונים קצרים

1. **עו"ש: הפקדה של היום הופיעה כ"צפויה" במקום "כבר ירדה"** — `vAccount()`
   חילק בין "חיובים והכנסות צפויים" ל"תנועות שכבר ירדו" לפי
   `chargeDate>=today`/`<today`, לא תואם ל-`CALC.balance()`/`available()`
   שמתייחסים ל-`chargeDate<=today` כ"כבר בפנים היתרה". הפקדה ליעד/קרן
   (`doDeposit`) נרשמת עם `chargeDate=today`, אז נפלה בטעות ל"צפויה". תוקן
   ל-`>today`/`<=today` בשני הסינונים כדי שיתאימו לסמנטיקה של החישוב.
2. **גרף מגמה בדף הבית: פיצול חיסכון מול קרן לעתיד** — `CALC.month()` מחזיר
   עכשיו גם `savingGoal`/`savingFund` (לפי `goalId`→`goal.type==='fund'`),
   לא רק `saving` מאוחד. עמודת החיסכון בגרף מוערמת משני חלקים (יעדים אישיים
   סגול למטה, קרן לעתיד טורקיז למעלה — בכוונה מוערמת, בניגוד להוצאות/חיסכון
   שנשארות זו-לצד-זו, כי שני אלה כן אותה משפחה). שורת המקרא מציגה עכשיו
   עד 3 פריטים (הוצאות/חיסכון/קרן לעתיד); "קרן לעתיד" מוצג רק אם יש בכלל
   יעד מסוג `fund`.

## טיפים טכניים לסביבה (חשוב לזכור!)

- **אין node/npm/gh זמינים ב-Bash tool בסביבה הזו.** לבדיקה מקומית משתמשים
  בסקריפט PowerShell שכתוב-לבד (HttpListener) לשרת סטטי, כי הנתיב
  לפרויקט מכיל עברית (בעיות encoding עם PowerShell WriteAllLines רגיל —
  צריך UTF8 בלי BOM ושורות LF בפירוש, לא CRLF, כדי לא לשבור diff). הסקריפט
  ב-scratchpad ונמחק בין sessions — לבנות מחדש אם נדרש.
  **חשוב:** `read_console_messages` **מצטבר** בין ניווטים באותו tab —
  לפתוח tab חדש כדי לקבל קריאת שגיאות נקייה, לא לסמוך על tab ישן.
  גם `javascript_tool` שומר scope בין קריאות נפרדות באותו tab — לתת שמות
  ייחודיים למשתנים (`const`) בכל קריאה, אחרת "already declared".
  לפעמים `mcp__Claude_Browser__computer screenshot` נכשל ("Browser pane
  not displayed") — לא לתקוע על זה, להשתמש ב-`get_page_text`/
  `javascript_tool` עם `getBoundingClientRect()` למדידות מדויקות במקום.
- **תמיד לענות בעברית** (המשתמש ביקש את זה מפורשות)
- **לא לגעת** בריפו `benny054727/guards-tracker` — פרויקט נפרד לגמרי, בלי שום
  קשר
- לפני כל שינוי בנוסחאות חישוב — כדאי להריץ/לעדכן את `test.html`
- אחרי כל push, לפעמים צריך רענון קשה בדפדפן (יש תיקון `no-store` ב-`sw.js`
  שאמור למנוע את זה מעכשיו; הגרסה עלתה ל-`financeme-v3` בסשן הזה)
- כל שינוי בקוד: לבדוק תחביר (node לא זמין — לבדוק דרך טעינה בדפדפן בפועל),
  17/17 ב-`test.html`, בדיקה חזותית ב-375px **וגם** ב-1900px (כרטיס רחב
  חושף בעיות שלא נראות במובייל — ראו "גרף מגמה" למעלה), ואז
  `git add/commit/push` לענף `claude/mini-personal-project-56d7e3`

## מבנה js/ (פוצל מ-app.js יחיד — סעיף תחזוקה מחוות דעת מקצועית)

| קובץ | תוכן |
|---|---|
| `data.js` | KEY, Supabase client+sync+auth, DEFAULT_CATS, blank/migrate/save/uid |
| `utils.js` | עזרי תאריך ומספר |
| `calc.js` | CALC (מנוע חישוב) + LOANS (מנוע הלוואות) |
| `recurring.js` | genRecurring — ייצור תנועות מהוראות קבע; genLoanPayments — אותו דבר להלוואות |
| `alerts.js` | alerts() — מנוע התראות |
| `views-home.js` | el/toast/esc/render (הדיספצ'ר), vHome |
| `views-account-cards.js` | vAccount, vCards |
| `views-expenses-savings.js` | vExpenses, vSavings, txRow/donut/catSummary (עזרים משותפים) |
| `sheets-entry.js` | sheet()/closeSheet() (המודאל הבסיסי), הוספה/עריכת תנועה, עורך קטגוריות |
| `sheets-recurring-goals.js` | סנכרון יתרה, הוראות קבע, הכנסה משתנה, יעדי חיסכון, הלוואות |
| `sheets-settings.js` | כל מסכי ההגדרות, כרטיסים, גיבוי/ייבוא/איפוס/מחיקת ענן |
| `setup.js` | אשף התקנה ראשונית |
| `main.js` | **חייב להיטען אחרון** — bootstrap (event listeners + init IIFE) |

**חשוב:** script רגיל בכל קובץ (לא `type="module"`) — כל הקבצים חולקים global
scope בדיוק כמו כשהיו קובץ אחד, כי האפליקציה נשענת על `onclick="fn()"` בתוך
HTML שנוצר דינמית (ES modules אמיתיים היו דורשים לחשוף כל פונקציה כזו בנפרד
על `window`, עשרות מקומות, קל לפספס אחת ולשבור כפתור בשקט). הסדר בין הקבצים
לא קריטי מלבד `main.js` שחייב אחרון — הכל כאן הצהרות שנקראות בפועל הרבה יותר
מאוחר. אם מוסיפים קובץ js/ חדש — לזכור לעדכן גם `index.html` וגם `test.html`.

## דברים חשובים לזכור להמשך

- **תמיד לענות בעברית** (המשתמש ביקש את זה מפורשות)
- **לא לגעת** בריפו `benny054727/guards-tracker` — פרויקט נפרד לגמרי, בלי שום
  קשר
- לפני כל שינוי בנוסחאות חישוב — כדאי להריץ/לעדכן את `test.html`
- אחרי כל push, לפעמים צריך רענון קשה בדפדפן (יש תיקון `no-store` ב-`sw.js`
  שאמור למנוע את זה מעכשיו)
- כל שינוי בקוד: לעשות `node --check js/<הקובץ הרלוונטי>.js`, לבדוק ב-Playwright headless אם
  רלוונטי, ואז `git add/commit/push` לענף `claude/mini-personal-project-56d7e3`
